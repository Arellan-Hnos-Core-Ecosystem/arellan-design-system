# Config para GitHub Packages — usado en CI y `pnpm publish`
# No afecta `pnpm install` local (workspace packages se resuelven localmente)
@arellan-hnos-core-ecosystem:registry=https://npm.pkg.github.com